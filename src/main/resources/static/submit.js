// script.js

document.getElementById("myLink").addEventListener("click", function(event) {
  event.preventDefault(); // リンクのデフォルトの動作をキャンセル

  // ここで必要な情報を取得して、適切な処理を行う
  // たとえば、フォームデータを取得してサーバーに送信するなど
});
